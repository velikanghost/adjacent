"use client";

import { AppKitButton } from "@reown/appkit/react";

/** Reown AppKit connect/account button — external wallets only. */
export function ConnectButton() {
  return <AppKitButton balance="hide" />;
}
